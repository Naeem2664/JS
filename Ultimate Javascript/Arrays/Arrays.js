

   /*  const myArray1 = ['Naeem', 'Ali', 'Ghaffar', 'Amir', 'Bilal'] */

         /* Length function is used to find total length of the array */
         //console.log(myArray1.length)

         /*  Push method is used to add an element in an array at the last and 
         Pop is used to delete an element from an array but from the last.
         Push and Pop methods both work on the LIFO principle */


    //myArray1.push('Bismillah')
    //myArray1.push('Bismillah')
    //myArray1.pop()


         /* Unshift is used to add element at the start of the array
         and shift is used to delete an element from array at starting position */

    //myArray1.unshift('Sardar')
    // myArray1.shift();
         /* For of loop is used to display element of an array 
         and For in loop is used to for index of an array*/

    //for(let element in myArray1){
       // console.log(element)
    //}

    //for (let element of myArray1) {
    //console.log(element)
    //}


         /* Loop and Arrays */
  /*   
    const myArray2=[
        {
            name:'Actor-1',
            payment:15000

        },

        {
            name:'Actor-2',
            payment:14000

        },

        {
            name:'Actor-3',
            payment:12000

        },

        {
            name:'Actor-4',
            payment:17000

        }
    ] */
/* 
    for(let i=0 ; i < myArray2.length ; i++){
        myArray2[i].payment=myArray2[i].payment-2000;
    }
    console.log(myArray2) */

         /* Filter method is used to filter element based on condition in an array */
/* 
    const salary=myArray2.filter((actor)=>actor.payment>12000)
    console.log(salary) */

    /* const myArray3=[1,2,3,4,5,6,7,8];

    const even=myArray3.filter((even)=>even%2===0)
    console.log(even)  */
